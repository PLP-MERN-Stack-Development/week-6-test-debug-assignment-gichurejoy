{
    "name": "mern-testing-server",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "jest",
        "test:unit": "jest --testPathPattern=unit",
        "test:integration": "jest --testPathPattern=integration",
        "test:e2e": "echo 'E2E tests are run from the root or client'"
    },
    "dependencies": {
        "express": "^4.18.0",
        "mongoose": "^8.16.4"
    },
    "devDependencies": {
        "jest": "^29.0.0",
        "mongodb-memory-server": "^8.0.0",
        "supertest": "^6.3.0"
    }
}
